<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude DataStore preferences (contains encrypted SMB credentials) from cloud backup -->
        <exclude domain="sharedpref" path="datastore"/>
    </cloud-backup>
    <device-transfer>
        <!-- Exclude DataStore preferences from device transfer -->
        <exclude domain="sharedpref" path="datastore"/>
    </device-transfer>
</data-extraction-rules>
